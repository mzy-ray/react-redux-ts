module.exports = {
  // omit parenthesis in arrow functions with a sole parameter
  arrowParens: 'avoid',
  // no space between brackets in object literals
  bracketSpacing: false,
  // maintain existing line endings
  endOfLine: 'auto',
  // true: Put > on the last line instead of at a new line
  jsxBracketSameLine: false,
  // use double quotes instead of single quotes in JSX
  jsxSingleQuote: false,
  // specify the line length
  printWidth: 100,
  // wrap prose as-is for services use a linebreak-sensitive renderer
  proseWrap: 'preserve',
  // use semicolons at the ends of statements
  semi: true,
  // use single quotes instead of double quotes
  singleQuote: true,
  // specify the number of spaces per indentation-level
  tabWidth: 2,
  // Indent lines with spaces instead of tabs
  useTabs: false,
  // use trailing commas wherever possible when multi-line
  trailingComma: 'es5',
  // parser: 'babylon',
  autoFixOnSave: true
};
